# Tasks (запланировано / на подумать)

Публичный файл — сюда попадают только задачи и открытые вопросы уровня всего проекта, без персональных данных клиентов (для них — `clients/<slug-клиента>/todo.md`). В отличие от `docs/todo.md` (чек-лист без дат), здесь каждая запись — со временной меткой, привязана к конкретному коммиту/сессии и попадает сюда, когда в коммите на это есть только ссылка (см. `README.md` → «Коммиты»).

Формат: `[дата время] — коммит/версия — что запланировано / над чем подумать`.

## 2026-08-27 04:58 IST — коммит 0.7.0

- **Запланировано:** создать в MySQL базу `health-coach-plan` (текущее значение `DB_NAME` в `.env`) — сейчас `npm run db:check` падает с `Unknown database 'health-coach-plan'`.
- **На подумать:** вернуть `NODE_ENV` в `.env`/`.env.example`, когда появится конкретная задача, где он реально нужен (например, различное логирование или выбор режима polling/webhook) — в 0.7.0 убрали как неиспользуемое.

## 2026-08-29 10:49 IST — коммит 0.8.0

- **Запланировано:** после того как анкета сохранена — генерировать и отправлять клиенту в боте готовую программу тренировок/план питания; пока бот только пишет ответы в БД и ничего не возвращает.
- **Запланировано:** для повторного прохождения анкеты (сверка прогресса через 2–3 недели) — увеличивать `questionnaire_answers.round` вместо перезаписи `round=1`; сейчас повторный `/start` тихо затирает старые ответы того же клиента.
- **На подумать:** пользователь `health` в MySQL сейчас имеет полные права на весь инстанс (включая `SUPER`, `CREATE USER`), а не только на базу `health-coach-plan` — это часть уже существующей общей dev-настройки, не создавалось в этой сессии, но стоит сузить права под отдельную учётку бота, когда база перестанет быть чисто dev-окружением.

## 2026-08-29 11:16 IST — коммит 0.9.0

- **Запланировано:** вписать реальный Telegram ID владельца в `OWNER_TELEGRAM_ID` (`.env`) и перезапустить бота — без этого роли `admins` пустые, `/addadmin`/`/removeadmin`/`/stats`/`/clients`/`/admins` никому не доступны.
- **Запланировано:** открыть веб-админку в браузере и пройтись по всем страницам вживую — пока проверялась только через `curl` (коды ответов и наличие данных в HTML), визуально не смотрели.
- **На подумать:** `/addadmin` по `@username` работает только для тех, кто уже хоть раз писал боту (username берётся из таблицы `clients`) — для человека, который боту вообще не писал, нужен его числовой Telegram ID.

## 2026-08-29 11:52 IST — коммит 0.10.0

- **Запланировано:** перенести группы клиентов и журнал правок ответов в веб-админку — сейчас управляются только командами бота.
- **Запланировано:** механизм периодической сверки прогресса (замеры/самочувствие раз в 2–3 недели) — свой раунд/формат, отдельно от `/extend` (который уже занял `round = 2` под расширение анкеты).
- **На подумать:** проверить весь новый функционал (продолжение анкеты, `/extend`, `/editanswer`, `/logs`, группы) вживую через реальный Telegram-чат — пока проверено только на уровне БД/логики напрямую.

## 2026-08-29 12:06 IST — коммит 0.11.0

- **Запланировано:** добавить в веб-админку редактирование текста ответа клиента (не только просмотр журнала правок) — по аналогии с `/editanswer` в боте.
- **На подумать:** веб-логин сейчас один общий для всех, кто знает пароль (без разделения по группам, в отличие от бота) — если понадобится несколько человек с разным доступом в веб-панели, нужен будет отдельный механизм авторизации вместо общего HTTP Basic Auth.

## 2026-08-29 12:23 IST — коммит 0.12.0

- **Запланировано:** проверить весь цикл «залы/оборудование» вживую — создать зал через `/creategym`, реально загрузить фото через `/addequipment`, посмотреть, что веб-админка (`/gyms/:id`) действительно показывает превью через фото-прокси.
- **На подумать:** связать вопрос анкеты про место тренировок с конкретной записью `gyms`, чтобы у тренера при открытии клиента сразу была ссылка на его зал и оборудование, а не отдельный текстовый ответ.

## 2026-08-29 12:40 IST — коммит 0.13.0

- **Запланировано:** проверить `/creategym` → `/addequipment` с настоящим фото вживую — что байты реально долетают до MinIO (не только до Telegram) и открываются в `/gyms/:id` в веб-админке.
- **На подумать:** бэкфилл в MinIO для фото оборудования, загруженного до этой версии (пока актуальных таких нет — база пустая после теста).

## 2026-08-29 12:52 IST — коммит 0.14.0

- **Запланировано:** классифицировать 36 фото зала «arena gym ncw» (#3) по классам оборудования — сейчас все со статусом «не классифицировано».
- **Запланировано:** добавить способ редактировать локацию зала после создания (сейчас нет ни в боте, ни в вебе) — «arena gym ncw» создан без адреса.
- **На подумать:** дедуп импорта сейчас по имени файла, а не по хэшу содержимого — если один и тот же файл переименуют и импортируют повторно, попадёт как дубликат; пока не проблема, но стоит иметь в виду.

## 2026-08-29 13:48 IST — коммит 0.15.0

- **Запланировано:** открыть `/gyms/3` в браузере и вживую проверить лайтбокс — открытие, пролистывание, переключение «Просмотр» ⇄ «Просмотр с редактированием», сохранение классификации прямо из галереи. Пока проверено только через `curl` (HTML/данные на странице корректные).
- **На подумать:** если понадобится больше возможностей lightGallery (zoom, миниатюры под лайтбоксом, счётчик слайдов) — сейчас подключён только core, без плагинов.

## 2026-08-29 14:06 IST — версия 0.15.0 (задача на будущее, код не менялся)

- **Запланировано: добавить ещё один зал из данных `/Users/Shared/assets/gym_nw`.**
  Контекст для новой сессии (полностью, без предположения, что предыдущий диалог помнят):
  - В той же папке `/Users/Shared/assets/gym_nw`, откуда раньше (см. запись 12:52 IST выше) уже импортировали зал «arena gym ncw» (#3) через `scripts/import_media.js`, с тех пор появились ещё два источника:
    - `equipment_catalog.json` — структурированный каталог: `gym_name: "The Arena Gym"`, 5 категорий (`categories[].name`: «Кардио-зона», «Свободные веса», «Тренажёры со свободным весом (плитозагружаемые)», «Селекторные (блочные) тренажёры», «Функциональный тренинг»), в каждой — массив `items` (24 штуки суммарно) с полями `id`, `name`, `brand`, `card_image` (относительный путь вида `cards/01_rowing_machine_concept2.png`), `description`, `quantity_seen`.
    - `cards/` — 24 уже обрезанных (без интерфейса Google Maps) фото, по одному на позицию каталога, имена файлов соответствуют `card_image` из JSON.
  - **Использовать для импорта — только `equipment_catalog.json` + `cards/`.** Папку `row_data/` (те же 36 сырых скриншотов, что уже лежали в `gym_nw` раньше и уже импортированы в зал #3) — **не анализировать и не использовать**, это явное указание пользователя.
  - `scripts/import_media.js` в текущем виде **не подходит напрямую** — он просто сканирует папку с картинками и берёт имя из имени файла (`humanizeName`), не читает JSON. Для этой задачи нужен либо новый скрипт (например `scripts/import_catalog.js`), либo расширение `import_media.js` опцией `--catalog equipment_catalog.json`, который:
    1. Читает JSON, по каждой категории создаёт (или переиспользует, если код уже есть) запись в `equipment_classes` — код можно сделать транслитерацией `category.id` (в JSON уже есть `id` на категориях и на items — латиницей, готово для `equipment_classes.code`/дедупа).
    2. Для каждого `item` заливает соответствующий файл из `cards/` в MinIO и создаёт `gym_equipment` с `name` = `item.name` (+ бренд, если есть — например `"${name} (${brand})"`), сразу проставляя `equipment_class_id` на созданный/найденный класс — то есть фото сразу приходят классифицированными, в отличие от прошлого сырого импорта.
    3. `item.description` и `quantity_seen` сохранить пока некуда — в `gym_equipment` таких колонок нет; либо завести миграцию с новыми nullable-колонками, либо просто дописывать `description` в конец `gym_equipment.name`, если заводить колонку не хочется ради одного зала.
  - **Уточнено у пользователя (2026-08-29 14:10 IST):** несмотря на то, что `equipment_catalog.json` называет зал так же похоже — `"The Arena Gym"` — это **действительно другой, отдельный зал**, не зал #3 «arena gym ncw». Создавать новую запись в `gyms`, а не дозаполнять #3. Имя для новой записи — взять `gym_name` из JSON («The Arena Gym») либо уточнить у пользователя более точное название/локацию перед созданием, если «The Arena Gym» покажется на практике слишком похожим на «arena gym ncw» и будет путать в списках (`/gyms`, `/gym <id>`).

## 2026-08-29 14:46 IST — версия 0.16.0

- **Сделано:** задача выше выполнена — `scripts/import_catalog.js` написан, зал #4 «The Arena Gym» создан, 24 позиции импортированы с готовой классификацией (5 классов из категорий каталога), описанием и количеством. Проверено целиком (MinIO, веб-страница, побайтовая сверка одного файла).
- **На подумать:** локация зала #4 тоже пустая (как и у #3 ранее) — в проекте по-прежнему нет способа отредактировать локацию зала после создания.

## 2026-08-29 14:58 IST — коммит 0.17.0

- **На подумать:** удаление клиента/зала в веб-админке — без ограничения по группе (веб-логин = полный доступ, как у владельца, тот же принцип, что уже применялся к `/logs`/`/clients`) — при желании разграничить, придётся вводить полноценную многопользовательскую авторизацию в веб вместо общего Basic Auth (см. запись 13:48 IST выше).
- **На подумать:** копирование данных клиента доступно на карточку любого клиента с дропдауном по всем остальным — при большом числе клиентов список стоит будет заменить на поиск/автокомплит вместо простого `<select>`.

## 2026-08-29 15:08 IST — коммит 0.17.1

- **На подумать:** мягкое удаление сейчас есть только у `clients` и `gyms` целиком — нельзя удалить отдельную единицу оборудования (`gym_equipment`) или отдельный класс, только весь зал. Если понадобится — тот же паттерн (`deleted_at` + фильтр в списках) несложно повторить.
- **На подумать:** «удалённых» пока нельзя удалить окончательно (очистить корзину) — если база разрастётся мягко удалёнными записями, может понадобиться отдельная функция полной очистки (с явным вторым подтверждением).

## 2026-08-29 15:45 IST — коммит 0.17.2

- **Запланировано:** пройтись по `/menu` и `/strategies/:code` реальным браузером — кликнуть каждый AJAX-контрол (сохранить/удалить/добавить/подвинуть/применить), убедиться, что интерфейс ведёт себя как задумано. Пока проверено только то, что разметка (data-атрибуты, id-контейнеры) на месте и те же HTTP-запросы, что шлёт JS, отрабатывают на бэкенде корректно — сам JS в браузере не запускался.
- **На подумать:** тот же AJAX-паттерн (`data-ajax`/`data-refresh`/`data-remove-row`) можно распространить и на другие страницы админки (`/clients`, `/gyms`, `/classes`, `/groups`), если захочется убрать перезагрузку и там — сейчас сделано только там, где просили (`/menu`, `/strategies`).

## 2026-08-30 02:48 IST — версия 0.18.0

- **Запланировано:** проверить `/newsurvey`, `/addmeasurement`, `/measurements` вживую через реальный Telegram-чат — пока весь цикл проверен только на уровне БД/прямых вызовов функций.
- **На подумать:** при осмотре `bot_menu_commands` обнаружено, что 8 команд (`whoami`, залы/оборудование) сейчас `active=0` — не наши правки в этой сессии. Нужно уточнить у пользователя, ожидаемо ли это, прежде чем включать обратно.
- **На подумать:** механизм периодической сверки прогресса теперь есть (замеры), но именно "новая программа по итогам `/newsurvey`" — генерация плана всё ещё не реализована (бот только сохраняет ответы), это отдельная, более старая задача.

## 2026-08-30 03:23 IST — версия 0.18.1

- **На подумать:** если понадобится реально прятать `/addmeasurement`/`/measurements` из меню отключённого клиента (не только мягкий отказ по нажатию) — потребуется per-chat `setMyCommands` для рядовых клиентов, сейчас это есть только для админов/владельца.

## 2026-08-30 IST — версия 0.19.0

- **Запланировано:** пройтись по новой странице раунда (`/clients/:id/surveys/:round`) и по всем пагинированным спискам реальным кликом в браузере — пока проверено только через `curl` (коды ответов, счётчики, разметка).
- **На подумать:** если `/strategies/:code` когда-нибудь разрастётся до по-настоящему длинного списка вопросов — постраничное разбиение там потребует другого UX (сейчас порядок вопросов двигается кнопками вверх/вниз по всему массиву сразу, что несовместимо с обычной пагинацией без правки этой логики).
- **На подумать:** 8 команд меню бота (`whoami`/`gyms`/`gym`/`creategym`/`addequipment`/`showequipment`/`classes`/`classifyequipment`) остаются выключенными в `/menu` — вопрос пользователю задавался (версия 0.18.0), ответа пока не было, само по себе к пагинации не относится.
