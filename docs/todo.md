# TODO (проектный уровень)

_Публичный файл — задачи по конкретным клиентам см. в `clients/<slug-клиента>/todo.md` (в `.gitignore`, не публикуется)._

## Проект в целом
- [ ] Заводить новых клиентов по инструкции в `README.md` / `clients/index.md`
- [ ] Раз в 2–4 недели по каждому клиенту — замеры/вес/фото, сверка прогресса (детали — в приватном todo клиента)
- [ ] Обновление версии в `package.json` при значимых изменениях подхода (новый клиент, новый этап, структурные правки) — с записью в `CHANGELOG.md` и `docs/decisions-log.md`

## Telegram-бот
- [x] Установить Node.js локально и проверить, что бот реально запускается (`npm install && npm start`)
- [x] Получить токен у @BotFather, добавить в `.env`
- [x] Реализовать сценарий онбординга через бота (имя/город/дата рождения → анкета из `survey_questions`, сохранение каждого ответа сразу в MySQL)
- [x] Решить, как бот сохраняет ответы клиента — в БД (`clients`, `questionnaire_answers`), не в markdown
- [ ] Сгенерировать и отправить клиенту готовую программу тренировок/план питания по итогам анкеты — сейчас бот только сохраняет ответы и ничего не возвращает
- [x] Продолжить недозаполненную анкету — `/continue`, плюс автоматически при любом сообщении вне активного диалога (в т.ч. на следующий день)
- [x] Пройти расширенную анкету после короткой — `/extend` (раунд 2, вопросы — разница `long` минус `short`)
- [x] Механизм периодической сверки прогресса — замеры (`/addmeasurement`, `/measurements`), напоминание раз в 2 недели от бота
- [x] Несколько опросников на клиента во времени — `/newsurvey` (новый раунд, `client_surveys`), старые раунды не пропадают
- [ ] Добавить базовую валидацию свободных числовых ответов (рост/вес — сейчас принимается любой текст)
- [ ] Определиться с режимом запуска (long polling vs webhook) и хостингом для продакшена

## Анкета (конфигурация в БД)
- [x] Вынести состав/порядок/текст вопросов из кода в MySQL (`survey_strategies`, `survey_questions`, `survey_strategy_questions`)
- [x] Кнопки-подсказки для вопросов с частым/типовым ответом, включая все да/нет-вопросы
- [x] Редактирование вопросов/стратегий без прямых SQL-запросов — веб-админка (`/questions`, `/strategies`)
- [ ] Решить, нужна ли третья стратегия/промежуточный вариант, когда накопится обратная связь от реальных клиентов

## Админка и роли
- [x] Веб-админка: список клиентов + их ответы, CRUD вопросов, состав/порядок вопросов в стратегиях (`src/admin/server.js`, `npm run admin`)
- [x] Роли в боте: `owner`/`admin`, команды `/stats`, `/clients`, `/admins`, `/addadmin`, `/removeadmin`, `/whoami`
- [x] Владелец назначен (`OWNER_TELEGRAM_ID` заполнен)
- [x] Админ может менять ответы клиентов (`/editanswer`) с записью в журнал (`answer_edit_log`)
- [x] Журнал правок доступен админам/владельцу (`/logs`) — владельцу все, админу — по своей группе клиентов + свои правки
- [x] Группы клиентов (`client_groups`) — владелец создаёт и назначает область видимости админам (`/creategroup`, `/setadmingroup`), любой админ распределяет клиентов (`/setclientgroup`)
- [x] Редактирование групп (переименование) в веб-админке (`/groups/:id/edit`)
- [ ] Веб-админку открыть в браузере и проверить вживую — пока проверялась только через `curl`
- [x] Перенести группы клиентов и журнал правок в веб-админку
- [ ] Добавить в веб-админку редактирование текста ответа клиента (сейчас только просмотр `/logs`, само редактирование — только `/editanswer` в боте)
- [ ] `docker-compose.yml`: сервис `admin` публикует порт на `0.0.0.0` (весь локальный роутер), а не только на `127.0.0.1` — приемлемо для dev-машины за NAT, но пересмотреть перед любым публичным хостингом (плюс HTTPS/reverse proxy)
- [ ] Подумать про `/addadmin` по `@username` для тех, кто ещё ни разу не писал боту (сейчас работает только по числовому Telegram ID или для тех, кто уже сделал `/start`)
- [ ] `/extend` жёстко завязан на коды стратегий `short`/`long` — обобщить, если появится третья стратегия
- [x] Меню бота настраивается из веб-админки по типу учётки (`/menu`, таблица `bot_menu_commands`) — с кнопкой живого применения в Telegram
- [x] Удаление клиентов и залов из веб-админки — мягкое (`deleted_at`), с восстановлением (`?deleted=1`); MinIO при удалении зала не трогается
- [x] AJAX-контролы на `/menu` и `/strategies/:code` (без перезагрузки страницы)
- [x] Таблицы в веб-админке не раздвигают страницу при длинном тексте (`.table-wrap`, `overflow-x: auto`)
- [x] Кнопки удаления в таблицах — единый вид «✕» (`/clients`, `/gyms`, `/strategies/:code`)
- [ ] Проверить AJAX-контролы (`/menu`, `/strategies/:code`) реальным кликом в браузере — пока только `curl`-проверка разметки и тех же запросов, что шлёт JS
- [x] Копирование данных анкеты одного клиента на другого с откатом через снимки (`client_snapshots`) — для тестов админа на выбранном клиенте
- [x] Копирование конкретного раунда анкеты любого клиента как нового раунда другому — аддитивно, не трогает существующие раунды получателя
- [x] Админ может выключить клиенту замеры и/или напоминания о них по отдельности (карточка клиента в веб-админке)
- [ ] Скрывать `/addmeasurement`/`/measurements` из персонального меню клиента, когда замеры выключены — сейчас команды в меню остаются, просто отвечают отказом (per-chat переопределение меню сейчас есть только для админов/владельца)
- [ ] Заметил, что 8 команд меню (`whoami`/`gyms`/`gym`/`creategym`/`addequipment`/`showequipment`/`classes`/`classifyequipment`) сейчас выключены в `/menu` — не я это сделал в этой сессии, уточнить у пользователя, специально или нет, и включить обратно, если нет

## Залы и оборудование
- [x] Сущности `gyms`/`equipment_classes`/`gym_equipment`, бот-команды (`/creategym`, `/addequipment`, `/classifyequipment` и т.д.) и веб-страницы (`/gyms`, `/classes`) с превью фото
- [x] Постоянное хранение фото в MinIO (`gym_media`, `scripts/setup_minio.js`) вместо только `photo_file_id` из Telegram
- [ ] Проверить вживую через реальную загрузку фото в Telegram — и сам факт сохранения, и что оно реально копируется в MinIO и открывается в веб-админке (пока — только на уровне БД/`curl` с сэмулированными байтами)
- [ ] Связать вопрос анкеты «где будешь тренироваться» с конкретным залом клиента — сейчас это просто текстовый ответ, к `gyms` не привязан
- [ ] Подумать про автоклассификацию оборудования по фото (сейчас — только вручную, кто загрузил или админ)
- [ ] Дать тренеру вставлять ссылку/фото оборудования прямо в текст программы — сейчас можно только посмотреть по `/showequipment <id>` или в веб-админке, программа тренировок всё ещё отдельный markdown-файл
- [ ] Бэкфилл MinIO-копий для фото, которые к моменту 0.13.0 уже были загружены только в Telegram (у текущих залов их пока нет — база с нуля после теста, но если появятся до апдейта — стоит досинхронизировать)
- [x] Массовый импорт медиа из локальной папки (`scripts/import_media.js`) — первый реальный зал «arena gym ncw» (#3) с 36 фото уже загружен и проверен
- [ ] Указать локацию зала «arena gym ncw» (#3) — при импорте адрес не спрашивался, сейчас пусто; сейчас в проекте вообще нет способа отредактировать локацию зала после создания (ни в боте, ни в вебе)
- [ ] Классифицировать 36 импортированных фото зала «arena gym ncw» по классам оборудования — сейчас все «не классифицировано». Теперь можно прямо в галерее (`/gyms/3` → «✏️ Просмотр с редактированием»)
- [x] Просмотр оборудования зала через lightGallery в веб-админке (`/gyms/:id`), 2 режима — обычный просмотр и просмотр с формой классификации прямо в лайтбоксе
- [ ] Проверить сам лайтбокс вживую в браузере (открытие/переключение слайдов/сохранение через «✏️ Просмотр с редактированием») — пока проверено только через `curl`, что HTML и данные на странице корректные
- [x] Второй зал «The Arena Gym» (#4) — 24 позиции из `equipment_catalog.json` + `cards/`, `scripts/import_catalog.js`, сразу с классами/описанием/количеством
- [ ] Указать локацию зала «The Arena Gym» (#4) — тот же пробел, что и у #3: адрес не задавался при импорте, редактировать локацию зала после создания пока негде (ни в боте, ни в вебе)

## БД и Docker
- [x] Установить Docker/включить демон и проверить `docker compose up --build` (сценарий с общей MySQL на машине, `DB_HOST=host.docker.internal`)
- [ ] Проверить вживую Compose-профиль `local-db` (свой изолированный MySQL-контейнер проекта) — пока протестирован только сценарий с общей MySQL на машине
- [x] Спроектировать и применить схему для анкеты (`clients`, `questionnaire_answers`, `survey_*`)
- [x] Выбрать инструмент миграций — простой раннер `.sql`-файлов (`scripts/migrate.js`), без внешней зависимости
- [x] Подключить `src/db.js` к реальному коду бота
- [ ] Для продакшена — сменить дефолтные пароли (`DB_PASSWORD`, `DB_ROOT_PASSWORD`) в `.env` на реальные, не хранить их нигде кроме `.env`/секрет-хранилища сервера
- [ ] Сузить права пользователя `health` в MySQL до одной базы `health-coach-plan` — сейчас у него полные права на весь инстанс (в т.ч. `SUPER`, `CREATE USER`), это осталось от текущей dev-настройки общей БД и избыточно
- [ ] Спроектировать схему для программ тренировок/планов питания (следующий шаг после того, как решим, как их генерировать) и перенести туда данные существующих клиентов из markdown
