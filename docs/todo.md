# TODO (проектный уровень)

_Публичный файл — задачи по конкретным клиентам см. в `clients/<slug-клиента>/todo.md` (в `.gitignore`, не публикуется)._

## Проект в целом
- [ ] Заводить новых клиентов по инструкции в `README.md` / `clients/index.md`
- [ ] Раз в 2–4 недели по каждому клиенту — замеры/вес/фото, сверка прогресса (детали — в приватном todo клиента)
- [ ] Обновление версии в `package.json` при значимых изменениях подхода (новый клиент, новый этап, структурные правки) — с записью в `CHANGELOG.md` и `docs/decisions-log.md`

## Telegram-бот
- [ ] Установить Node.js локально и проверить, что бот реально запускается (`npm install && npm start`) — не тестировался
- [ ] Получить токен у @BotFather, добавить в `.env`
- [ ] Реализовать сценарий онбординга: 15 вопросов из `templates/questions-template.md` через бота (Telegraf scenes/wizard)
- [ ] Решить, как бот сохраняет ответы клиента — вручную в `clients/<slug>/`, или сразу в БД (см. "На будущее")
- [ ] Продумать выдачу клиенту готовой программы тренировок/питания через бота
- [ ] Определиться с режимом запуска (long polling vs webhook) и хостингом для продакшена

## На будущее
- [ ] Вынести данные клиентов (карточки, анкеты, программы, планы питания) из markdown-файлов в базу данных — выбор конкретной БД пока не сделан, определимся позже
