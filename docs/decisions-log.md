# Лог решений (проектный уровень)

Формат записи: `[дата время] — решение — обоснование — версия package.json`. Время — по местному времени клиента (IST, Ирландия), см. `templates/`.

Этот файл — **публичный**, репозиторий открытый: сюда попадают только решения об архитектуре и процессе проекта. Решения, касающиеся конкретного клиента (его цели, программа, план питания), фиксируются в `clients/<slug-клиента>/decisions-log.md` — эта папка в `.gitignore` и наружу не публикуется.

## 2026-08-25 23:06 IST — версия 0.1.0

- **Решение:** заложена структура проекта (анкета клиента, программа тренировок, план питания, логи решений/вопросов/задач) и `package.json` для версионирования.
- **Обоснование:** проект будет дорабатываться итеративно (новые данные клиентов, корректировки программ по прогрессу), нужен фундамент, который не придётся перестраивать при росте проекта.

## 2026-08-25 23:26 IST — версия 0.2.0

- **Решение:** проект переведён на мультиклиентскую структуру: `client/` и `program/` заменены на `clients/<slug-клиента>/` (своя папка на каждого клиента — карточка, анкета, программа, план питания), добавлен `clients/index.md` как реестр.
- **Обоснование:** клиентов несколько, каждому нужна отдельная папка вместо одной общей.
- **Решение:** заведена первая карточка клиента (см. `clients/index.md` — данные не публикуются, репозиторий публичный).

## 2026-08-26 06:40 IST — версия 0.3.0

- **Решение:** шаблон анкеты вынесен из `docs/` в отдельную папку `templates/` (`templates/questions-template.md`) — клиенты и переиспользуемые шаблоны физически разнесены по разным папкам верхнего уровня (`clients/` vs `templates/` vs `docs/`).
- **Решение:** папка `clients/` целиком добавлена в `.gitignore` — персональные данные клиентов (имена, города, здоровье, антропометрия) не должны попадать в git-историю.
- **Обоснование:** явный запрос — разнести клиентов и шаблоны по разным папкам и не пускать данные клиентов в git.
- **Заметка на будущее:** зафиксирована задача перенести все данные (клиенты, анкеты, программы, планы питания) из markdown в базу данных; конкретная СУБД пока не выбрана — см. `docs/todo.md`.

## 2026-08-26 06:46 IST — версия 0.4.0

- **Решение:** репозиторий будет публичным — все записи с персональными данными клиентов (имя, город, здоровье, антропометрия, история решений по конкретному человеку) вычищены из `docs/decisions-log.md` и `docs/todo.md` и перенесены (не удалены — контекст сохранён) в приватные `clients/<slug-клиента>/decisions-log.md` и `clients/<slug-клиента>/todo.md`.
- **Решение:** добавлен `CHANGELOG.md` в формате Keep a Changelog — публичная история версий проекта без персональных данных.
- **Решение:** во все файлы с историей (логи решений, todo, таблицы «История версий» в программах/планах) добавлены временные метки (дата + время), не только дата.
- **Решение:** из `package.json` убрано поле `author` с личным email — не нужно для проекта и не должно светиться в публичном репозитории.
- **Обоснование:** явный запрос — убрать персональные данные из проекта перед тем, как репозиторий станет публичным.

## 2026-08-26 07:52 IST — версия 0.5.0

- **Решение:** сделан первый git-коммит (`docs/`, `templates/`, `package.json`, `README.md`, `CHANGELOG.md`, `.gitignore` — без `clients/`).
- **Решение:** выбран стек для Telegram-бота — **Node.js + Telegraf**. Рассматривались варианты Node.js + grammY и Python + aiogram; выбор сделан клиентом.
- **Решение:** добавлен минимальный каркас бота: `src/bot.js`, `src/config.js`, `.env.example`. `.env` добавлен в `.gitignore` — токен бота не должен попасть в публичный репозиторий.
- **Обоснование:** проект будет работать через Telegram — нужен рабочий скелет бота, на который можно наращивать сценарий анкеты и выдачу программ.
- **Статус:** сценарий онбординга (15 вопросов) через бота ещё не реализован — бот пока отвечает только на `/start` и `/help`. Node.js на машине разработчика не установлен — локально не запускался и не тестировался.

## 2026-08-26 08:03 IST — версия 0.6.0

- **Решение:** проект завёрнут в Docker — добавлены `Dockerfile` (образ бота) и `docker-compose.yml` (сервис `bot` + опциональный сервис `mysql` за Compose-профилем `local-db`).
- **Решение:** выбрана MySQL как СУБД (закрывает часть задачи "перенести данные в БД" из `docs/todo.md` — выбор конкретной СУБД сделан, схема и миграция данных клиентов — ещё нет).
- **Решение:** подключение к БД настраивается одним и тем же `.env` (`DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD`/`DB_SSL`) для трёх сценариев без изменения кода: локальный MySQL-контейнер этого проекта (свой volume и порт `DB_LOCAL_PORT`, не конфликтует с другими проектами на машине), MySQL, установленная на машине разработчика (общая на все проекты), MySQL на удалённом VPS (с `DB_SSL=true`). Логика — `src/config.js` (чтение и разбор переменных) и `src/db.js` (пул соединений через `mysql2`).
- **Обоснование:** явный запрос — завернуть проект в Docker и дать удобный единый конфиг для подключения к MySQL, с поддержкой и локальной, и VPS-базы.
- **Статус:** подключение к БД пока нигде в коде бота не используется (`src/db.js` не импортируется в `src/bot.js`) — это задел на будущее, когда появится реальная работа с данными. Docker-образ не собирался и не тестировался — на машине разработчика не установлен Docker/Node.js.

## 2026-08-27 04:36 IST — версия 0.7.0

- **Решение:** добавлен `scripts/check-db.js` и npm-скрипт `db:check` для самостоятельной проверки подключения к MySQL текущими настройками `.env`, без запуска самого бота.
- **Решение:** `BOT_TOKEN` больше не проверяется в `src/config.js` при загрузке модуля — проверка перенесена в `src/bot.js`. Раньше `config.js` бросал исключение при пустом `BOT_TOKEN`, и это ломало любой скрипт, использующий `src/db.js` (он require'ит `config.js`), даже когда токен бота ему не нужен — в частности, не давало проверить БД отдельно от бота.
- **Решение:** в `.env`/`.env.example` для настроек бота выделен отдельный подписанный блок «Telegram-бот» (аналогично уже существующему блоку БД) с комментарием, где взять `BOT_TOKEN`. Переменную `NODE_ENV` сначала добавили про запас, но убрали в том же коммите — решили не держать в `.env` то, что пока нигде не используется в коде, добавим её, когда появится конкретная задача (логирование, polling/webhook).
- **Обоснование:** явный запрос — продолжить разработку: дополнить `.env` настройками бота и добавить отдельный скрипт проверки подключения к БД, вынесенный в `package.json`. Скрипт проверен вживую — подключился к реальной MySQL на 127.0.0.1:3306 и корректно сообщил об отсутствующей базе `health` (сама база ещё не создана; локально в `.env` `DB_NAME` затем вручную сменили на `health-coach-plan` — оставлено личным override, шаблон `.env.example`/`docker-compose.yml` по решению не синхронизировали).
- **Решение:** зафиксирована конвенция коммитов (`README.md` → «Коммиты»): текст коммита — только «что сделано» (как и раньше), а «что запланировано» и «над чем подумать» в сам коммит не идут — фиксируются с временной меткой в новом `tasks.md`, из коммита на них короткая пометка (`; задачи — tasks.md`).
- **Обоснование:** явный запрос — коммит должен явно разделять сделанное/запланированное/вопросы на подумать, но при этом укладываться в лимит длины сообщения.

## 2026-08-27 05:08 IST

- **Решение:** переписаны сообщения двух коммитов в истории (`ec464b8` → `Добавлен каркас Telegram-бота...`, `4a37259` → `Бот завёрнут в Docker, добавлен единый конфиг подключения к MySQL`) — повелительное наклонение заменено на причастие, чтобы вся история соответствовала действующей конвенции. Содержимое коммитов (файлы, дерево) не менялось, только текст сообщений и, соответственно, хэши этих двух коммитов и всех последующих. Первый коммит (`2de01fb`) не трогали — он не был в повелительном наклонении.
- **Обоснование:** явный запрос — исправить несоответствие старой истории новой конвенции коммитов.
- **Статус:** переписано только локально. `origin/main` на GitHub (репозиторий публичный) всё ещё указывает на старые хэши — для синхронизации нужен `git push --force`, что требует отдельного подтверждения.

## 2026-08-29 10:49 IST — версия 0.8.0

- **Решение:** сценарий анкеты (9→17 вопросов в зависимости от стратегии) реализован через `Scenes.BaseScene` (Telegraf), а не через `WizardScene` — состояние ведётся вручную по `ctx.scene.state.step`, т.к. число шагов зависит от выбранной стратегии и заранее неизвестно на старте сцены.
- **Решение:** состав, порядок и текст вопросов анкеты вынесены из кода в MySQL (`survey_strategies`, `survey_questions`, `survey_strategy_questions`, см. `migrations/`) — явный запрос: чтобы можно было настраивать анкету (в т.ч. переключать «короткая/длинная» стратегию) без правок кода и деплоя.
- **Решение:** добавлен лёгкий раннер миграций (`scripts/migrate.js`, таблица `migrations` — тот же формат `id/name/run_on`, что уже использовался для более ранней миграции `clients`/`questionnaire_answers`, применённой до появления этого раннера в репозитории), без внешней зависимости типа `db-migrate` — простой перебор `.sql`-файлов, каждый применяется один раз.
- **Решение:** вопрос «пол/возраст/рост/вес» разбит на три отдельных вопроса (пол, рост, вес) — принцип «один вопрос за один шаг»; возраст убран совсем, т.к. дата рождения уже спрашивается в карточке клиента и однозначно даёт возраст.
- **Решение:** там, где у вопроса есть частый/типовой ответ (в т.ч. любой да/нет-вопрос) — бот показывает Telegram reply-кнопки с вариантами; поле ввода при этом не блокируется, свой текст всегда можно напечатать вместо кнопки — явный запрос («пользователь ленив»).
- **Решение:** бот подключён к уже существующей на машине разработчика MySQL (контейнер `vps_local-mysql-1`, база `health-coach-plan`, пользователь `health`) через `DB_HOST=host.docker.internal` — это тот же сценарий 2 из README («MySQL уже установлена на машине»), только теперь бот сам тоже в Docker, а не на хосте напрямую. Миграции создавали только новые таблицы/колонки — существовавшие в базе клиенты и ответы не тронуты.
- **Обоснование:** явный запрос — сделать анкету настраиваемой без деплоя, снизить трение для отвечающего (кнопки), развернуть проект в Docker и проверить его в реальной работе с телефона.
- **Статус:** выдача готовой программы тренировок/питания клиенту через бота по итогам анкеты — всё ещё не реализована (анкета только сохраняет ответы), см. `docs/todo.md` и `tasks.md`.

## 2026-08-29 11:16 IST — версия 0.9.0

- **Решение:** сделаны два независимых входа для администрирования — веб-админка (`src/admin/server.js`, HTTP Basic Auth логином/паролем из `.env`) и роли прямо в боте (таблица `admins`, команды `/stats`/`/clients`/`/admins`/`/addadmin`/`/removeadmin`) — явный запрос, две разные точки входа, а не одна общая.
- **Решение:** права в боте — `owner` (один, назначается через `OWNER_TELEGRAM_ID` в `.env`, закрепляется при старте бота) и `admin` (может назначать других `admin`, но не может удалять). Удалять администраторов может только `owner` — так и запрошено.
- **Решение:** доступ к веб-админке не завязан на роли `admins` из бота — это отдельный общий логин/пароль (`ADMIN_WEB_USER`/`ADMIN_WEB_PASSWORD`), т.к. в вебе нет понятия «текущий telegram-пользователь», через который можно было бы проверить роль.
- **Решение:** редактирование анкеты в веб-админке ограничено CRUD вопросов и управлением их составом/порядком в стратегиях (добавить/убрать/подвинуть ▲▼) — без визуального drag-n-drop и без клиентского JS, чтобы не тащить сборку фронтенда ради внутреннего инструмента на несколько страниц.
- **Обоснование:** явный запрос — веб-админка для редактирования анкеты и просмотра клиентов, плюс роли обычный пользователь/админ/владелец в самом боте с управлением через команды.
- **Статус:** веб-админка проверена через `curl` (авторизация, CRUD вопросов, список клиентов) — не проверена вживую в браузере. `OWNER_TELEGRAM_ID` не заполнен (не было под рукой Telegram ID владельца) — пока никто не назначен владельцем/админом, `/addadmin` и `/removeadmin` некому вызвать, кроме как через прямое редактирование таблицы `admins`.

## 2026-08-29 11:52 IST — версия 0.10.0

- **Решение:** «продолжить анкету» реализовано через БД, а не через сессию в памяти — при возврате клиента (в т.ч. на следующий день, когда Telegraf-сессия из памяти уже не сохранилась, например после перезапуска бота) состояние восстанавливается из `questionnaire_answers`/`clients`, а не полагается на `ctx.scene.state`, который переживает только текущий процесс.
- **Решение:** любое сообщение клиента вне активного диалога (не только `/start`/`/continue`) подхватывает недозаполненную анкету — иначе бот на такое сообщение никак не реагировал (глобального обработчика текста не было вовсе).
- **Решение:** «расширение» короткой анкеты до полной сохраняется отдельным раундом (`round = 2`) в `questionnaire_answers`, а не перезаписывает раунд 1 — чтобы не потерять уже данные ответы и не переиспользовать те же `question_number` для разных по смыслу вопросов.
- **Решение:** правки ответов админом идут через ту же функцию сохранения, что и сам сценарий анкеты (`saveAnswer` в `src/clients.js`, теперь общая), плюс отдельная запись в `answer_edit_log` — старое/новое значение, кто и когда поменял.
- **Решение:** группы клиентов — `group_id = NULL` у админа означает «видит всех», а не отдельный флаг «доступ ко всем группам» — так владельцу не нужно поддерживать список «все группы» при добавлении новой группы, снятие ограничения = один `UPDATE`.
- **Решение:** назначать клиента в группу может любой админ (организационная задача, не вопрос доступа), а вот кто из админов какую группу видит — только владелец (`/setadmingroup`), это и есть контроль доступа.
- **Обоснование:** явный запрос — можно пройти расширенную анкету после короткой, продолжить недопройденную (в т.ч. на следующий день через меню), админы могут править ответы клиентов с логированием, группы клиентов с ограничением видимости админов по группе, владелец видит все логи.
- **Статус:** все новые функции проверены на уровне данных/логики (создание клиента, полное прохождение короткой + расширение до полной, правка ответа с записью в лог, фильтрация по группе) — не проверено вживую через реальный Telegram-чат. Веб-админка группы и логи пока не показывает.

## 2026-08-29 12:06 IST — версия 0.11.0

- **Решение:** группы и журнал правок добавлены в веб-админку — переиспользуют те же функции (`clients.setClientGroup`, `admins.setAdminGroup`, `audit.listEditLogs`), что и команды бота, никакой отдельной бизнес-логики для веба не заводилось.
- **Решение:** в вебе `/logs` и списки клиентов не ограничиваются группой — веб-логин один общий (`ADMIN_WEB_USER`/`ADMIN_WEB_PASSWORD`), не привязан к конкретному telegram-админу и его `group_id`, так что ограничивать нечем и не по чему.
- **Решение:** смена области видимости админа для строки `owner` в `/admins` защищена не только скрытием формы в HTML, но и на уровне запроса — `admins.setAdminGroup` не трогает роль `owner` (проверено вручную: прямой POST в обход интерфейса не смог изменить `group_id` владельца).
- **Обоснование:** явный запрос — раз уже добавили группы и журнал правок в бота, сделать то же самое доступным и из веб-админки.
- **Статус:** проверено через `curl` — создание группы, назначение клиенту, попытка обойти защиту владельца (не сработала, как и задумано). Редактирование текста ответа клиента через веб не добавлено — только просмотр `/logs`.

## 2026-08-29 12:23 IST — версия 0.12.0

- **Решение:** три сущности вместо двух — `gyms` (зал), `equipment_classes` (класс/таксономия оборудования, справочник) и `gym_equipment` (конкретная единица в конкретном зале с фото) — явный запрос: одно и то же оборудование должно быть представимо в разных залах, значит класс должен быть отдельной переиспользуемой сущностью, а не полем прямо на фото.
- **Решение:** фото не скачиваются и не хранятся в своей инфраструктуре — только `photo_file_id`, который Telegram и так хранит бессрочно у себя; лишний файловый стораж (S3/MinIO — на машине уже есть контейнер MinIO от другого проекта, но трогать чужую инфраструктуру не стали) не заводили.
- **Решение:** веб-админка не встраивает прямую ссылку Telegram (`getFileLink`) в `<img src>` — та ссылка содержит токен бота открытым текстом в URL, что утекло бы любому, кто откроет исходный код страницы. Вместо этого сервер сам скачивает байты и отдаёт их через свой урл (`/gyms/:gymId/equipment/:id/photo`) — токен наружу не уходит.
- **Решение:** «обработка фото» на этом этапе — это ручная классификация человеком (загрузившим фото или админом), а не автоматическое распознавание оборудования на изображении; про автоматизацию явного запроса не было, а подключать vision-модель без обсуждения — необоснованное расширение задачи. Схема данных это не блокирует — при появлении автоклассификатора он просто заполнял бы то же поле `equipment_class_id`.
- **Обоснование:** явный запрос — отдельная сущность «залы», которые заводит клиент или админ (привязка к локации или типовые), с фото оборудования, из которого формируется база оборудования зала (ещё одна сущность), плюс класс оборудования как отдельное понятие, потому что одно и то же оборудование бывает в разных залах.
- **Статус:** проверено на уровне данных и через `curl`, включая деградацию фото-прокси при недействительном `file_id` (502, не падение процесса). Реальная загрузка фото через настоящий Telegram-чат не проверялась — нужно тестировать вживую.

## 2026-08-29 12:40 IST — версия 0.13.0

- **Решение:** используем уже существующий на машине MinIO (проект `vps_local`, сеть `local_shared`) вместо своего — явный запрос пользователя; в прошлой версии (0.12.0) решили этого не делать без явного запроса, теперь запрос есть.
- **Решение:** контейнеры этого проекта подключаются к внешней докер-сети `local_shared` (`docker-compose.yml`, `networks: local_shared: external: true`) — это единственный способ дотянуться до MinIO: у него нет порта, опубликованного на хост, только сетевой доступ внутри докера. `scripts/migrate.js` по-прежнему работает с хоста (у MySQL есть host-порт), а `setup_minio.js` — только изнутри контейнера.
- **Решение:** "папка на зал" в MinIO — это префикс ключа (`gym/<id>/`) с пустым `.keep`-объектом для видимости в консоли MinIO, а не настоящая директория (у S3-совместимых хранилищ директорий не существует в принципе).
- **Решение:** отдельная таблица `gym_media` вместо замены `gym_equipment.photo_file_id` на новый столбец — так `gym_equipment` остаётся привязан к Telegram (откуда физически приходит фото), а `gym_media` — к MinIO (где оно физически хранится постоянно); одно от другого не зависит, MinIO-копия может не удаться, а Telegram-версия при этом всё равно работает.
- **Решение:** копирование в MinIO — best-effort с логированием ошибки, не блокирует сохранение фото через Telegram, если MinIO в моменте недоступен.
- **Обоснование:** явный запрос — bucket `health-coach-plan`, папка `gym`, своя папка на каждый зал, таблица маппинга медиа на MinIO, всё через отдельные скрипты `setup_minio`.
- **Статус:** полный цикл (зал → оборудование → загрузка в MinIO → чтение обратно → отдача через веб-прокси) проверен с сэмулированными байтами напрямую через `src/media.js`, без настоящего Telegram-фото — байты совпали побитово. Нужно проверить с реальной загрузкой через бота.

## 2026-08-29 12:52 IST — версия 0.14.0

- **Решение:** массовый импорт — отдельный CLI-скрипт (`scripts/import_media.js`), а не команда бота: 36 файлов через Telegram-чат по одному было бы неудобно, а бот и не читает файлы с диска. Переиспользует те же `src/gyms.js`/`src/equipment.js`/`src/media.js`, что и бот/веб — никакой отдельной логики создания зала или загрузки в MinIO для CLI не заводили.
- **Решение:** идемпотентность через `gym_equipment.source_file` (имя файла) вместо, например, хэша содержимого — папка с фото зала на практике не будет содержать переименованных дублей чаще, чем случайно одинаково названные разные файлы; более сложный вариант (хэш) можно добавить позже, если понадобится.
- **Решение:** `gym_equipment.photo_file_id` сделан необязательным — импортированное с диска фото физически не имеет Telegram `file_id`. Везде, где раньше слепо использовался `photo_file_id` (пересылка фото в боте, прокси в вебе), добавлена ветка «нет file_id → берём байты из MinIO».
- **Решение:** папка для импорта монтируется в контейнер `bot` только на чтение (`:ro`) — скрипт не должен и не может менять исходные файлы на хосте.
- **Обоснование:** явный запрос — загрузить фото для первого зала («arena gym ncw») из `/Users/Shared/assets/gym_nw`, обернуть через отдельный скрипт `import_media`, который получает путь и название/id зала.
- **Статус:** реальный импорт выполнен и проверен целиком — зал #3 «arena gym ncw» (тип `location`, локация не указана — не спрашивалась при импорте), 36 фото, все объекты в MinIO (`gym/3/equipment/`), один файл сверен побайтово через веб-прокси с оригиналом на диске — совпадение полное. Классификация фото по классам оборудования — ещё не сделана, это ручной следующий шаг.

## 2026-08-29 13:48 IST — версия 0.15.0

- **Решение:** код группы клиентов в веб-редактировании не меняется, только название — тот же принцип, что уже применялся к коду вопроса анкеты (`/questions/:id/edit`): код используется как стабильный идентификатор в других местах (`/setclientgroup`, `/setadmingroup` в боте), менять его задним числом опаснее, чем кажется.
- **Решение:** lightGallery — фактически JS-виджет без сборки: CDN-скрипт/CSS, `dynamic: true` + программный массив `dynamicEl`, а не разметка `<a>`-тегов с data-атрибутами — так проще было реализовать переключение между двумя режимами показа (обычным и с формой редактирования) одним и тем же виджетом, без пересоздания разметки страницы.
- **Решение:** режим редактирования встраивает обычную HTML-форму (select класса + текстовое поле + кнопка) прямо в `subHtml` слайда и сохраняет через `fetch()` на тот же POST-роут, что уже использовался для формы под карточкой (до этой версии) — второй, отдельный API-эндпоинт не заводили, чтобы не дублировать логику сохранения.
- **Решение:** `licenseKey` для lightGallery не задавали — консольное предупреждение в GPLv3-конфигурации ожидаемо для не-коммерческого/непубличного использования (внутренний инструмент, не поставляется третьим лицам), покупать/подделывать лицензионный ключ не стали.
- **Обоснование:** явный запрос — редактирование групп в админке, и лайтбокс-просмотр оборудования зала на базе lightGallery с двумя режимами (просмотр / просмотр+редактирование).
- **Статус:** проверено через `curl` — переименование группы (код не изменился), сохранение классификации через тот же эндпоинт, что теперь дергается и из JS в открытой галерее; страница `/gyms/3` отдаёт корректный HTML с 36 миниатюрами, подключённым lightGallery и встроенными данными. Взаимодействие с самим лайтбоксом (открытие, переключение слайдов, клик «Сохранить» из браузера) вручную в браузере не проверялось — стоит открыть и попробовать глазами.

## 2026-08-29 14:46 IST — версия 0.16.0

- **Решение:** каталожный импорт — отдельный скрипт (`import_catalog.js`), не режим/флаг у `import_media.js` — входные данные принципиально другие (структурированный JSON с категориями и метаданными против плоской папки картинок), общий код (поиск/создание зала, загрузка в MinIO, дедуп по `source_file`) вынесен в переиспользуемые функции (`gyms.findOrCreateGym`, уже существовавшие `equipment.findBySourceFile`/`media.uploadEquipmentPhoto`), а не скопирован.
- **Решение:** категории каталога становятся классами оборудования один в один (`category.id` → `equipment_classes.code`, `category.name` → `name`) — в самом каталоге они уже осмысленно поделены (кардио/свободные веса/тренажёры/функциональный тренинг), плодить свою классификацию поверх готовой не стали.
- **Решение:** для `description`/`quantity_seen` завели нормальные nullable-колонки в `gym_equipment`, а не совали в `name` через дефис — данные из каталога содержательные и потенциально полезны тренеру отдельно от названия (например, «замечено 2 экземпляра» — это не то же самое, что название единицы оборудования).
- **Решение:** бренд, если он определён (`brand !== "не определён"`), добавляется в отображаемое название («Гребной тренажёр (Concept2)») — так в списках/галерее видно бренд без отдельного поля и без изменения структуры отображения, которое уже было (`gym_equipment.name`).
- **Обоснование:** явный запрос — доделать задачу из предыдущей записи (14:06 IST) после того, как пользователь подтвердил через уточняющий вопрос, что это действительно отдельный, второй зал.
- **Статус:** реальный импорт выполнен и проверен целиком — зал #4 «The Arena Gym», 24 позиции, 5 классов оборудования созданы автоматически из категорий каталога, все объекты в MinIO (`gym/4/equipment/`), один файл сверен побайтово через веб-прокси — совпадение полное.

## 2026-08-29 14:58 IST — версия 0.17.0

- **Решение:** меню бота вынесено в БД (`bot_menu_commands`) той же трёхуровневой моделью tier'ов, что уже была в коде (client/admin/owner) — не изобретали новую модель ролей под редактируемое меню, просто дали редактировать существующую.
- **Решение:** веб-админка применяет меню в Telegram живьём сама (свой `Telegraf(...).telegram` клиент, тот же, что уже используется для фото-прокси) — не через сам процесс бота. Это разные процессы (`bot`/`admin` — отдельные контейнеры), а `setMyCommands` — обычный вызов Bot API по токену, отдельный работающий бот-процесс для этого не требуется.
- **Решение:** применение изменений меню в Telegram — по отдельной кнопке «Применить сейчас», не автоматически на каждое сохранение поля — чтобы правка нескольких команд подряд не била лишний раз по Bot API и у админа был явный контроль, когда именно меню обновится у живых пользователей.
- **Решение:** копирование данных клиента — только «содержимое» анкеты (стратегия/статус/ответы), не сама личность учётки (имя/город/Telegram/группа) — иначе это было бы слияние двух разных людей в одну запись, а не «примерить чужие тестовые данные», как просили.
- **Решение:** откат — через полный снимок состояния получателя перед копированием (JSON: профиль + все ответы), а не через попытку «отменить» конкретную операцию копирования — так один и тот же механизм работает для отката после N копирований подряд (снимок снимается каждый раз заново, историю снимков видно и можно восстановить любой, не только последний).
- **Обоснование:** явный запрос — настраиваемое из админки меню по типу учётки, удаление клиентов/залов, копирование данных клиента с возможностью отката для тестов админа.
- **Статус:** всё проверено вживую (не только через `curl`, но и прямыми вызовами функций и сверкой с реальным Telegram Bot API): смена меню + live-применение без регрессии в наборе команд, копирование+откат (ответы получателя корректно вернулись), удаление клиента и зала (включая зачистку MinIO). По пути найден и исправлен баг сериализации даты в снимках (см. CHANGELOG 0.17.0) — пойман тестом до того, как дошло до реального использования.

## 2026-08-29 15:08 IST — версия 0.17.1

- **Решение:** удаление клиента/зала переведено с физического `DELETE` на флаг `deleted_at` — явный запрос: «удаление — флаг в базе, физически запись не удаляется». `getClient`/`getGym` намеренно не фильтруют по этому флагу (карточке и восстановлению нужно видеть запись в любом состоянии), фильтруют только списочные запросы (`listClients`/`listGyms`) и статистика.
- **Решение:** удаление зала больше не чистит MinIO — раз запись не удаляется физически, восстановление должно возвращать зал ровно как было, включая все фото; версия 0.17.0 успела прожить меньше часа с физическим удалением файлов, пересмотрено сразу по этому запросу.
- **Решение:** `upsertClient` при повторном визите клиента снимает `deleted_at` через `ON DUPLICATE KEY UPDATE` — раз `telegram_id` уникален, INSERT всё равно попадёт в существующую (пусть и помеченную удалённой) строку по этому ключу; если не снимать флаг явно, клиент продолжил бы анкету, но остался бы невидим в админке — нашли этот сценарий до того, как он стал багом в проде, а не после.
- **Обоснование:** явный запрос пользователя — сделать удаление мягким (флагом), не физическим.
- **Статус:** полный цикл удалить → скрыт из списка → виден в «Удалённых» → восстановить → снова виден, проверен и для клиента, и для зала (включая то, что MinIO-файлы зала пережили удаление). Автоматическое снятие флага при повторном обращении клиента к боту — тоже проверено отдельным тестом.

## 2026-08-29 15:45 IST — версия 0.17.2

- **Решение:** AJAX сделан как общий обработчик поверх существующих POST-роутов (data-атрибуты на форме), а не переписыванием каждого роута под JSON-ответы — все нужные действия и так уже отвечают редиректом на актуальную страницу, `fetch` с `redirect: 'follow'` (поведение по умолчанию) этим спокойно пользуется; после успеха просто заново запрашиваем страницу в фоне и подменяем нужный контейнер, а не ждём от сервера структурированный ответ.
- **Решение:** для строк-удалений — мгновенное удаление DOM-узла (`data-remove-row`) без ожидания повторного запроса; для действий, меняющих состав/порядок списка (добавить/подвинуть вопрос в стратегии, добавить команду в меню) — полный rerender конкретных контейнеров, а не попытка вручную собрать новую строку в JS (сервер и так уже умеет рендерить актуальное состояние, дублировать эту логику на клиенте — источник рассинхронизации).
- **Решение:** таблицы оборачиваются в `overflow-x:auto`-контейнер везде в админке, а не только там, где сейчас заметили проблему (`/strategies/short`) — тот же класс инлайн-форм с несколькими кнопками в ячейке действий есть и в других таблицах (`/clients`, `/gyms`), рано или поздно всплыло бы и там.
- **Решение:** крупные отдельно стоящие кнопки удаления на карточках клиента/зала не стали делать «✕» — иконка без подписи вне табличной строки контекста не даёт понять, что она делает; «✕» унифицирован именно для компактных построчных действий в таблицах, как и было явно указано в запросе.
- **Обоснование:** явный запрос — AJAX-контролы на `/menu` и `/strategies`, починка съезжающей таблицы на `/strategies/short`, единообразная кнопка «✕» для удаления везде.
- **Статус:** бэкенд-роуты не трогали (те же POST с редиректом) — пересверил add/move/remove в `/strategies/short` теми же запросами, что теперь шлёт AJAX, состояние до/после совпало и было восстановлено обратно. Сама разметка/скрипт проверены через `curl` (нужные data-атрибуты, id-контейнеры, обёртки таблиц на месте), реальный клик в браузере — нет, стоит пройтись глазами.

## 2026-08-30 02:48 IST — версия 0.18.0

- **Решение:** `round` 2 остаётся жёстко зарезервирован под `/extend`, а не переиспользуется — новый раунд (`/newsurvey`, копирование раунда от другого клиента) всегда стартует с `getNextRound() = GREATEST(MAX(round), 2) + 1`. Это проще и надёжнее, чем помечать «какой раунд для чего использован» динамически: не нужно ничего резервировать заранее для клиента, который никогда не расширял анкету — слот просто пропускается.
- **Решение:** `client_surveys` — отдельная таблица метаданных раунда, а не расширение `questionnaire_answers` новыми колонками — сами ответы и их структура (client_id/round/question_number) не изменились вообще, просто появилась параллельная таблица «что это был за раунд» (тип/стратегия/статус/даты). Так не пришлось трогать ни одну существующую функцию чтения/записи ответов.
- **Решение:** `clients.survey_strategy`/`status` остались как «текущее/последнее состояние» (обновляются при завершении любого раунда), не стали переводить всё на вычисление из `client_surveys` на лету — это потребовало бы правки каждого места, что сейчас читает эти поля (бот, админка, статистика), а `client_surveys` и так даёт полную историю тем, кому она нужна.
- **Решение:** резюме недозаполненной анкеты (`/continue`, пассивный перехват в `bot.on('text')`) обобщено на «любой `in_progress` раунд из `client_surveys`», не только round 1 — иначе `/newsurvey`, прерванный на середине, невозможно было бы корректно продолжить (старая логика решила бы, что анкета уже пройдена, раз round 1 завершён).
- **Решение:** замеры — гибкий справочник (`measurement_types`) вместо фиксированных колонок веса/талии/etc — явный запрос «пользователь сам выбирает параметры», значит набор параметров должен быть расширяемым без миграции на каждый новый параметр.
- **Решение:** напоминание о замерах — простой `setInterval` раз в сутки внутри процесса бота, не node-cron и не отдельный воркер — единственная периодическая задача в проекте, отдельная инфраструктура ради неё избыточна. Логика «кому пора» и «не спамить повторно» — целиком в SQL-запросе (`listClientsDueForReminder`), не в JS.
- **Решение:** в веб-админке два РАЗНЫХ действия копирования сосуществуют: старое «Скопировать данные» (перезаписывает весь профиль анкеты + все раунды получателя, со снимком для отката — для тестов админа на выбранном клиенте) и новое «Добавить анкету... новым раундом» (аддитивно, не трогает существующие раунды — для переиспользования чужой анкеты как отправной точки). Разные сценарии используют разную семантику, объединять их в одну кнопку было бы потерей информации о намерении.
- **Обоснование:** явный запрос — админам копировать `questionnaire_answers` любого клиента через веб, несколько опросников на клиента во времени (новые параметры → новая программа), механизм замеров с самостоятельным ежедневным вводом и двухнедельными напоминаниями от бота.
- **Статус:** весь цикл проверен на уровне БД/curl — раунды (short→long, резерв под extend соблюдён), замеры (запись, история, все три ветки логики напоминаний), копирование раунда между клиентами (дважды подряд, без потери прежних данных). Реального Telegram-диалога не запускал.

## 2026-08-30 03:23 IST — версия 0.18.1

- **Решение:** два независимых флага (замеры / напоминания), не один общий «выключить замеры» — явный кейс, когда это разумно разделить: клиент сам хочет вести историю замеров, но не хочет, чтобы бот напоминал (или наоборот — тренер хочет мягко напомнить, но конкретно этот клиент ответственный и сам заходит без напоминаний).
- **Решение:** блокировка команд — мягкая (ответ «отключено, обратись к тренеру»), а не скрытие их из кнопки меню — скрытие потребовало бы per-chat переопределения меню для КАЖДОГО клиента (сейчас `setMyCommands` со scope по chat_id используется только для админов/владельца, не для рядовых клиентов вообще), это отдельная, более тяжёлая механика ради вторичного косметического эффекта.
- **Обоснование:** явный запрос — админам в веб-панели выключать замеры и напоминания о них для клиентов.
- **Статус:** проверено — оба флага переключаются независимо через форму на карточке клиента, `listClientsDueForReminder` учитывает `measurement_reminders_enabled` верно в обеих комбинациях (замеры выкл/напоминания вкл и наоборот).

## 2026-08-30 IST — версия 0.19.0

- **Решение:** ответы анкеты вынесены на отдельную страницу по раунду (`/clients/:id/surveys/:round`) вместо плоского списка на карточке клиента — явный запрос: при накоплении раундов (потенциально до сотни) один список ответов всех раундов сразу становится непригодным для использования, нужен отдельный уровень роутинга.
- **Решение:** размер страницы задаётся в коде (`PAGE_SIZES` в `src/admin/pagination.js`), отдельно на каждую сущность, а не через БД/UI-настройку — явный запрос «для каждой сущности предусмотреть отдельно настройки пагинации» относился к тому, что размеры должны быть независимыми, не к тому, что их нужно редактировать через интерфейс; заводить для этого отдельную БД-сущность ради константы сочли избыточным.
- **Решение:** на страницах с несколькими списками сразу (карточка клиента — раунды/замеры/снимки) каждый список получил свой query-параметр номера страницы (`roundsPage`/`measurementsPage`/`snapshotsPage`), а не общий `?page=` — иначе перелистывание одного списка сбрасывало бы позицию другого.
- **Решение:** функции чтения списков (`listClients`, `listAdmins`, `listGyms`, `listGroups`, `listClientSurveys`, `listEditLogs`, `listGymEquipment`, `listClasses`, `listAllQuestions`) получили необязательные `limit`/`offset` — без `limit` поведение не изменилось (возвращают всё, как раньше), это нужно внутренним вызовам, которым нужен полный список целиком (дропдауны выбора, `syncAllAdminMenus`, поиск зала по имени при импорте, сборка меню бота, поиск незавершённого раунда анкеты). Так пагинация в веб-админке не потребовала правок ни одного из этих мест.
- **Решение:** `/strategies`, список вопросов внутри `/strategies/:code` и `/menu` пагинацию не получили — они короткие и курируемые, а логика «подвинуть вопрос вверх/вниз» в `/strategies/:code` работает над всем массивом вопросов сразу (индексы в массиве), постраничное разбиение сломало бы перестановку вопросов с разных страниц друг с другом.
- **Обоснование:** явный запрос — показывать ответы анкеты только когда выбран раунд, добавить уровень в роутинг, и пагинацию для всех данных, которые потенциально могут превысить 25 значений на экране, с отдельной настройкой размера страницы для каждой сущности.
- **Статус:** проверено на реальных данных — клиент с 3 раундами (41 ответ, тот самый со скриншота пользователя): плоская таблица ответов исчезла с карточки, ссылки на раунды ведут на новую страницу с верными метаданными/ответами/счётчиком; несуществующий раунд отдаёт 404; зал с 36 позициями оборудования (размер страницы 24) — 2 страницы, на первой ровно 24 карточки, lightGallery видит только текущую страницу; счётчики на `/clients` (4 активных / 3 в корзине) сверены напрямую с БД. Реального клика в браузере не делал — только `curl` по всем затронутым маршрутам.
